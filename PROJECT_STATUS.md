# SEAM Project Status

Last updated: 2026-06-02 (HISTORY#285 - wire in the browser dashboard: `seam serve`/`seam webui` serve it same-origin; deleted `experimental/` entirely)

## Current State

SEAM is operating as a local machine-first memory runtime with:

- MIRL compile/verify/persist/search/context flows in production use
- External memory benchmark registry + `seam bench external` CLI alias (Track I SOP 0 landed)
- SEAM LoCoMo adapter with string-match scoring + 60s quickstart fixture (Track I SOP 1 landed; `seam bench external --quickstart locomo`)
- Track M P0 standard benchmark readiness: top-level `seam bench external locomo|longmemeval|beam` dataset dry-run routing, mem0 harness adapter contract, pgvector composite-PK upgrade migration, LoCoMo source-evidence retrieval fix, and publication readiness gate requiring non-stub judge plus BIL-2 verification.
- Benchmark Integrity Level Phase 1 support: deterministic BIL-0 inspection plus BIL-1/BIL-2 benchmark sealing, verification, and inspection via `seam bench seal|verify|inspect`
- Full Textual interactive TUI dashboard with chat panel, command palette (/, !, ?), MIRL animation, independently scrollable panes, IDE-style explorer tree, status bar, colored RichLog panels, focus zoom toggle, runtime-smoked Settings tab, and live Overview health bars for database, pgvector, API/config, and settings paths
- Dashboard chat with expanded OpenRouter model defaults (Qwen, DeepSeek, MiMo, Kimi, GLM, Claude, Gemini, Grok, Gemma, Pareto Code Router)
- First interactive agent-style CLI shell: `seam shell` / `seam chat` for persistent memory remember/search/context/stats/doctor workflows
- lossless SEAM-LX/1 compression with integrity verification
- SEAM-HS/1 Holographic Surface PNG snapshots with automatic source-to-MIRL surface compile, direct MIRL/RC query, verify, decode, context, and import commands
- HS/1 surface library adapters landed on `main`: stored surface metadata, stable `hs:<hash>` IDs, list/show/repair, redundant file-backed copies, and direct query/verify/decode/context/import by stored ID
- Roadmap now includes Track G5: a planned zero-ops multi-surface `.seam.png` library index plus drift verifier, replayed from the completed local `roadmap/g5-multi-surface-index` branch onto current `main`.
- Cat4 single-hop attribution audit is recorded at `docs/audits/2026-05-31-cat4-single-hop-attribution.md`: capture-adapter/RAW-vs-abstract crowding is not the measured lever; pack budget is the dominant retrieval-context-recall throttle and needs judged validation before product claims.
- Semantic recovery policy/diagnostic experiment is recorded at `docs/audits/2026-06-01-semantic-recovery-policy-experiment.md`: explicit default-off LoCoMo adapter flags now label pack-budget/deep-candidate runs, saved reports/events include retrieval-policy diagnostics, and the no-paid grid measured baseline `0.623668`, pack-budget `0.682864`, deep-candidates `0.624195`, and pack-budget-deep `0.758217` context_recall_mean.
- Paid LoCoMo slice validation is recorded at `docs/audits/2026-06-01-paid-locomo-slice-validation.md`: a balanced 100-case OpenAI answerer+judge A/B improved judge_score_mean from `0.44` baseline to `0.57` for pack-budget-deep, with zero judge errors. This supports a full paid A/B only if the operator accepts the spend.
- benchmark diff tooling, pass/fail gate tooling, publish-only holdout fixture routing, and tracked CI coverage
- optional FastAPI/Uvicorn REST API surface for local compile, search, context, stats, health, persist, and lossless-compression workflows, with bounded request bodies and authenticated remote-bind guardrails
- agent-safe MCP stdio server for Gemini/Claude/Cursor-style MCP clients, plus legacy JSON-lines bridge, with compact memory search/get, ingest, stats, document status, context packing, doctor, stored HS/1 surface operations, and benchmark run summaries
- PgVector support running locally via Docker Compose on port 55432; installer coverage across Windows/Linux paths
- Linux installer supports two explicit modes: default global command install under `~/.local/share/seam`, and `--dev` repo-local Python bootstrap with the external-drive `.venv/lib64` fallback plus SEAM protocol verification; WebUI setup remains separate.
- Competitive RAG/install polish in progress on `codex/competitive-rag-install-polish`: one-line private install docs, product-first README, document status tracking, progressive memory search/get, `retrieve --mode vector|graph|hybrid|mix`, stdio agent bridge, and vector stale-index reporting
- Active/inactive code and docs separation enforced via `docs/CODE_LAYOUT.md`, `.rgignore`, and archive paths
- The browser dashboard is the functional SEAM GUI, served by the REST server itself: `seam serve` (or `seam webui`, which also opens the browser) serves `seam_runtime/webui/dashboard.html` at `/` on the same origin as the API — no Node, Vite, or CORS at runtime. `dashboard.html` is a self-contained in-browser React app (React+Babel from CDN, no build step). The Vite/React dev project lives at `webui/` (its `publicDir` points at `seam_runtime/webui/`); its `src/` panes remain rework material, but the shipped working UI is the static dashboard. `experimental/` has been removed entirely.
- Adaptive SEAM Skill Factory foundation is merged on `main` through PR #21; current tracked code includes Skill Factory primitives and roadmap docs under `seam_runtime/skills/` and `docs/roadmap/SKILL_FACTORY.md`.

## Current Resume Point

- `main` is the source-of-truth branch. After pulling, verify local `HEAD` equals `origin/main` before starting new work.
- Latest continuity handoff is `HISTORY#287` - **fixed `seam doctor` reporting `Streams: unavailable` from the console-script entry point.** `seam_runtime/doctor.py` `check_streams()` imported `tools.streams.verify_streams` directly, but the `seam` console script does not put the source-checkout root on `sys.path` and `tools/` is a sibling of the package that is intentionally not shipped in the wheel, so `seam doctor` (and the dashboard doctor surface) reported `Streams: unavailable (No module named 'tools')` unless run via `python -m` from the repo root or with `PYTHONPATH` set. Added `_import_streams_verify_all()` which, on `ModuleNotFoundError`, derives the repo root package-relative (`Path(__file__).resolve().parent.parent`) and inserts it into `sys.path` **only when** `tools/streams/verify_streams.py` actually exists there - a genuine wheel install still reports `unavailable` (no false positive), and resolution is deliberately not cwd/`git`-dependent. Regression test `test_doctor_streams_resolves_repo_tools_from_console_script` runs `check_streams()` in a subprocess from a non-repo cwd with `PYTHONPATH` stripped (an in-process assertion is useless since pytest runs from the repo root where `tools` already imports). Verified: bare `seam doctor` from `/tmp` now prints `Streams: PASS`; full gate `pytest test_seam_all tools/history/test_history_tools.py tools/streams/test_streams.py` = 417 passed / 0 failed with pgvector up. Surfaced while relocating the working checkout off the T7 external drive to `/home/terrabyte/Documents/Projects/Seam` (internal ext4); doctor behaved identically on both copies, so it is pre-existing, not relocation-induced.
- Prior handoff `HISTORY#286` - **wired the SEAM-augmented dashboard chat end-to-end and fixed two bugs found in audit.** The dashboard agent panel now chats THROUGH SEAM (retrieve memory → inject as system prompt → call the selected provider) via a new `POST /chat` in `seam_runtime/server.py` (OpenAI-compat + Anthropic schemas, stdlib `urllib`; key order: dashboard key → server env → `local` for Ollama), with `SeamAPI.chat()` and the `dashboard.html` send handler rewired off `context()`. **BUG 1 (fixed):** `/chat` read `rec.get("text")` but MIRL records carry no `text` field, so memory was counted (`memory_used>0`) yet never injected; now renders each candidate via the public `iter_textual_fields(record)` (the same text the embedder indexes) and counts only records that produced text. **BUG 2 (fixed):** retrieval ran outside the try/except, so a vector-backend outage returned a raw 500 on the default `use_memory=true` path; now degrades to a no-memory answer (`memory_used=0` + `memory_error`) and the dashboard shows a "memory unavailable" note. Tests `tests/audit/test_chat_endpoint.py` 5 → 7 (regression guards for both bugs); verified 7 passed, server/api/webui subset 21 passed, live SQLite-adapter harness shows `memory_used=2` with content injected and pgvector-down returns 200 not 500. **Known follow-ups:** server does not auto-load `OPENAI_API_KEY` from `.env.local` (env-key fallback needs a manual export); injected memory text is the indexed slug form, not original NL; a real successful provider reply is still unverified (no local model pulled, no cloud spend while operator away).
- Prior handoff `HISTORY#285` - **wired the browser dashboard in as the functional SEAM GUI and deleted `experimental/` entirely.** Operator decision: SEAM is used either via this dashboard or the CLI; nothing is experimental. Moved `experimental/webui/` → repo-root `webui/` (React/Vite dev project, kept intact) and the served static assets → `seam_runtime/webui/` (packaged); deleted `experimental/`. Wired FastAPI (`create_app`) to serve `dashboard.html` at `/` plus its same-origin assets via a static mount added AFTER the API routes (so `/health` etc. still win); added `webui_dir()` + `_mount_webui()` and a `seam webui`/`seam dashboard-web` command that serves + opens the browser. `dashboard.html` calls the API with relative paths, so one `seam serve` delivers UI + API (no Node/Vite/CORS). Pointed `webui/vite.config.ts` `publicDir` at `../seam_runtime/webui` (one canonical asset copy); added `webui/*` to pyproject package-data. **Browser-verified with Playwright:** found and fixed a real blocker — an HTML comment (`<!-- -->`) inside the inline JSX crashed the Babel transpile and left the page blank; converted it to a `/* */` JS comment, after which the full IDE dashboard renders (explorer, editor, agent chat, terminal, status bar) and `/health`+`/seam-api.js` serve 200. Cleaned stale `experimental` references in `mcp.py` error text, a test docstring, `installers/README.md`, `docs/setup.md`, `docs/CODE_LAYOUT.md`. **Known follow-up:** the `webui/src/` React rewrite is still incomplete rework material (the shipped UI is the static `dashboard.html`); finishing/replacing it is future work.
- Prior handoff `HISTORY#284` - fixed the `experimental/` misrepresentation and deferred packaging. `experimental/retrieval_orchestrator/` was NOT experimental — it is load-bearing runtime code imported by `seam_runtime/{cli,mcp,dashboard,benchmarks}.py` (6 sites). Promoted it via `git mv` to `seam_runtime/retrieval_orchestrator/` (internal imports are relative so they survived; updated 15 import lines across 9 files incl. tests + `tools/ci/chroma_real_smoke.py` + the `BlockRetrievalOrchestrator` MetaPathFinder string); removed vestigial `experimental/__init__.py`; set pyproject `packages.find` to `seam_runtime*` only; updated `docs/CODE_LAYOUT.md`. `experimental/` now holds ONLY the non-Python `experimental/webui/` TypeScript dashboard prototype (separate question, untouched). Packaging/release is deferred to new **ROADMAP Track N — Packaging, Release, and Distribution** (private distribution, keep `Do Not Upload`, name `seam-runtime`, license change to Apache-2.0/MIT pending operator). Full CI suite green on Linux (exit 0); Windows re-verified by CI on the PR. **Full CI matrix (ubuntu + windows) is green as of #282/#283.**
- Prior handoff `HISTORY#283` - after #282 dropped Windows `test-and-benchmark` from `10 failed, 6 errors` to `1 failed, 881 passed`, the lone remaining failure was `tests/audit/test_pool_concurrency.py::test_concurrent_writes_all_commit` raising `OperationalError('database is locked')` — pre-existing Windows write-concurrency flakiness, independent of #282 (the test is self-contained with its own tmp db + pool). Root cause: the test built its pool with a bare `sqlite3.connect(db, timeout=5)` factory that did NOT enable WAL, so it exercised a less-robust config than production `SQLiteStore` (which sets `journal_mode=WAL` + `busy_timeout=5000`). Fixed by establishing WAL in `_bootstrap` and giving the pool a `_wal_connect` factory (WAL + busy_timeout), mirroring production. On the same PR #51 as #282. CI re-verifies Windows.
- Prior handoff `HISTORY#282` - Windows SQLite-handle cleanup so `test-and-benchmark (windows-latest)` can go green (it was red with `[WinError 32] file in use` on ~10 benchmark/surface/mem0 tests; Linux unlinks open files so it never showed there). Root cause: transient `SeamRuntime`/`SQLiteStore`/per-scope LoCoMo adapter runtimes were not closed before their temp DBs/dirs were deleted; on Windows an open SQLite handle locks the file. Added `SeamRuntime.close()` + context-manager and `SeamLocomoAdapter.close()` + context-manager; closed `surface_store`, persist `temp_runtime`/`reopened_runtime`, and the two agent-family `temp_runtime` finally blocks in `seam_runtime/benchmarks.py`; closed the mem0 harness runtime after `add`/`search`; and fixed `_is_ephemeral_path` to recognize POSIX `/tmp`,`/var/tmp` on Windows (where `Path("/tmp").resolve()` becomes `C:\tmp`). Two direct-construction tests (`test_benchmark_reproducibility`, `test_cross_encoder_rerank`) now close their adapters. Verified on Linux (no regression; affected slices 38 passed); Windows-only behavior is verified via CI on the PR. **Known follow-up:** the parallel LoCoMo runner (`run_benchmark_grouped_parallel`) creates factory adapters it does not close — not covered by any CI test, but real Windows parallel runs could still leak; owed a runner-side close.
- Prior handoff `HISTORY#281` - benchmark integrity-hash regression fix. The advisory `test-and-benchmark` CI matrix job had been RED on `main` since 2026-05-31 (1 failing test, independent of #280): `benchmarks/external/common/runner.py` attached `answerer_diagnostics` to the case under `--save-context` (added by #278) but did not exclude it from `integrity_exclude_keys`, so a save-context run produced a different `integrity_hash` than the equivalent default run. Added `"answerer_diagnostics"` to the exclude set; only save-context runs change, normal/sealed bundles are byte-identical. Lands on the same branch as #280 (PR #50) so `test-and-benchmark` can go green. The three required ruleset checks were already passing.
- Prior handoff `HISTORY#280` - full-audit closeout. All four protocol gates (integrity/continuity/routing/streams) pass, `main` matched `origin/main`, and the full suite is green (exit 0) with pgvector up. Fixed a local test-harness leak: an operator shell that exports `SEAM_PGVECTOR_DSN` routed every persist-path test to the optional Docker pgvector backend, so ~21 non-pgvector tests failed with raw `connection refused` when the container was down. New `tests/conftest.py` autouse fixture hides the ambient DSN for non-`external` tests (they run on SQLite, matching CI); `tests/audit/test_pgvector_real_adapter.py` is now marked `external` so it keeps the ambient DSN and self-gates via skipif. Resolved three stale remote branches: deleted `claude/remote-control-AD6Di` (stale PROJECT_STATUS snapshot from #220) and `roadmap-trust-security-manual` (Track K doc already harvested into main's evolved version); kept `backup/local-pgvector-bootstrap` (deliberate backup holding files absent from main). The two `in-progress` history entries #225/#247 are append-only and already superseded by #226/#248 — protocol-correct as-is, not edited. **Operator clarified the Track K gate: "benchmarks 100% functional" means the benchmark results can systematically/automatably improve the system — i.e. the closed observe->propose->apply->re-measure self-improvement loop, which is still OPEN per #274 (no auto-proposer, no apply step; retrieval reads hardcoded weights + env flags, not a stored policy). Tests-green is necessary but not that bar.**
- Prior handoff `HISTORY#279` - paid 100-case OpenAI LoCoMo slice completed for baseline vs pack-budget-deep. Results: judge_score_mean `0.44 -> 0.57`, correct_count `32 -> 40`, incorrect_count `44 -> 26`, zero judge errors, all major categories improved. Raw paid reports are outside git under `../Seam-artifacts/20260601-paid-locomo-slice/`. This is a positive slice, not full LoCoMo proof; the next gated step is the full 1542-case paid A/B if the operator accepts the spend.
- Prior handoff `HISTORY#278` - semantic recovery policy flags and diagnostics landed for the LoCoMo SEAM adapter, with full no-paid pack-budget/deep-candidate reports archived outside git under `../Seam-artifacts/20260601-semantic-recovery-policy/`. The result confirms pack budget is the dominant retrieval-context-recall throttle: deep candidates alone barely move recall, while pack-budget-deep reaches `0.758217` (`+0.134549` vs baseline). This is not yet answer-quality evidence; the next gated step is a paid judged baseline-vs-pack-budget-deep comparison.
- Prior handoff `HISTORY#277` - Cat4 single-hop attribution audit preserved as an active audit doc, generated `diag_out/` raw artifacts moved outside the repo to `../Seam-artifacts/20260601-192239-cat4-diag_out/`, and `.gitignore` now ignores future `diag_out/` dumps. The audit resolves the #274 open question: the measured cat4 gap is not abstract-record crowding/capture-adapter displacement; pack budget and judged validation are the next lever.
- Prior handoff `HISTORY#276` - local branch/PR closeout: `main` was clean and matched `origin/main`; GitHub had zero open PRs; `recovery/locomo-dataset-271` had no unique patch left after PR #43; `roadmap/g5-multi-surface-index` had one completed roadmap commit but was based before later audit work, so its intended G5 card was replayed onto current `main` without merging stale derived files. No runtime code changed.
- Prior handoff `HISTORY#275` - CI regression fix for #274: the mocked `PgVectorAdapter` cursor in `test_seam_all/test_seam.py` unpacked the INSERT into 7 values; #274's new `namespace` column made it 8, breaking 5 ubuntu-CI tests. Fixed the fake cursor to unpack 8 (incl. namespace). **Lesson: the suite has two roots — `tests/` AND `test_seam_all/test_seam.py` — and CI runs both; always run `pytest tests/ test_seam_all/test_seam.py` before pushing schema/SQL changes.** No production code changed. Substance is #274 below.
- Prior handoff `HISTORY#274` - **substream isolation (leak-seal).** `runtime.search_ir` gained an `ns` param that confines BOTH the candidate load (`load_ir(ns=...)`) and the vector top-K (new `namespace` column + filtered `search` on both SQLite and pgvector backends) to a single namespace. The sealed leak: `search_ir` previously filtered candidates by `scope` only + global vector search, so a multi-namespace single store leaked another namespace's records (masked in LoCoMo by per-conversation dbs). `ns=None` is byte-identical to prior behavior; gated for A/B by `SEAM_RETRIEVAL_SCOPED_VECTORS` (`RetrievalFlags.scoped_vectors`), end-state ON for multi-tenant stores. Demonstrate-then-seal regression test in `tests/audit/test_substream_isolation.py`. Validated: scoped-pgvector full-10-conv `0.623668` ≡ the per-scope-isolated SQLite oracle `0.623668` exactly (partition key correct), ~0 recall vs unscoped `0.626719`. **Two gap-analysis findings recorded as the next levers:** (1) **single-hop cat4** (n=841, recall ~0.73) is the biggest retrieval-fixable lever — 89% of gold answers are fully present in-conversation, headroom ≈ +0.065 global; open: ranking-miss vs packing-displacement (needs one `--save-context` diagnostic). (2) **self-improvement loop is OPEN** — observe (`retrieval_event` records gold-hit/ranks/scores/context_recall) + validate + gate + re-measure exist, but there is **no auto-proposer and no apply step** (retrieval reads hardcoded weights + env flags, not a stored policy); `RetrievalFlags` are the seed of the apply step. Also corrected the stale `docs/audits/2026-05-28-locomo-retrieval-memory.md` (banner: RRF/reranker measured to regress, scope-search ~0 recall). Supersedes #273 below.
- Superseded handoff `HISTORY#273` - audit #3 retrieval-scoring levers, implemented behind three default-OFF env flags (`SEAM_RETRIEVAL_SEMANTIC_ZERO` / `_BM25_ALL` / `_RRF`) in `seam_runtime/retrieval.py` (`RetrievalFlags`) and wired through `runtime.search_ir`; flag-OFF is byte-identical to the locked baseline (`context_recall_mean = 0.528308`). Measured on the real pgvector Docker backend (exact NN search + `--workers 1` = deterministic given table contents; the shared `seam_vector_index` table must be TRUNCATEd between runs; noise floor ±0.002 = one borderline question). **R1 (semantic→0 when a live vector backend omits a record from the top-K, instead of bag-of-words fallback) is the only principled win:** full 10-conv (n=1542) baseline `0.626719` → R1 `0.631354` (+0.0046 global), with cat1 multi-hop +0.026 (identical across conv-26/5-conv/10-conv) and cat3 open-domain +0.018, at a small cat4 single-hop −0.004 cost. R2 (BM25-all-kinds), R3 (RRF), and the cross-encoder reranker regress or add noise-level gains with displacement. Per operator decision the infra landed default-OFF; **flipping R1 globally vs category-gating it for multi-hop/open-domain queries is an open operator lever.** Architectural follow-up surfaced: pgvector vector search is scope-UNFILTERED (single shared `seam_vector_index`, global top-K), so cross-scope vectors crowd in-scope records out of the top-K in multi-conversation runs — owed a scope-namespaced-search slice. Supersedes #271 below.
- Superseded handoff `HISTORY#271` - LoCoMo dataset recovery + permanent anti-loss safeguards. The LoCoMo dataset (`locomo10.json`) had been lost (it lived only on the 90%-full root volume, not T7; removed during an operator mount operation). Re-acquired canonical from snap-research/locomo (sha256 `79fa87e9…`, 2805274 bytes, 10 convs, 1986 QA), verified authentic against surviving fragments (this release relabels sample_ids: its `conv-30` == the prior release's `conv-26`; 103/104 fragment questions matched). Permanent safeguards landed: dataset committed in-repo at `benchmarks/external/locomo/data/locomo10.json` (on T7 + offsite via GitHub), `locomo10.manifest.json` pins URL+sha256+counts, and `tools/benchmarks/restore_locomo.py` restores+SHA-verifies from in-repo → T7 `.dataset_store/locomo/` → network (`--verify/--ensure/--to`). The canonical dataset is relocated off the root volume onto T7 per operator decision. No-paid 100-case slice (`--answerer none --judge none`, local bge-small embeddings, SQLite vector backend) reproduced `context_recall_mean = 0.528308`, an exact match to the HISTORY#242 baseline `0.5283084138` — proving byte-identity of the restored data, an intact harness, and SQLite==pgvector equivalence (no-paid path no longer needs the Docker pgvector service). Cleared 23 stale mislabeled per-scope DBs from `test_seam/locomo/`. **audit #3 (retrieval scoring) is now UNBLOCKED** (dataset restored) but not started. The full ~1542-case judged run remains paid + operator-gated and should follow the audit #3 fixes. Supersedes #270 below.
- Prior handoff `HISTORY#270` - SQLite concurrency-layer hardening on `audit/concurrency-hardening-270` (uncommitted in working tree at handoff time). Continues #269: `seam_runtime/pool.py` checkout() split into `_acquire`/`_release` so connection validation runs OFF the global lock (F3) and the blocking-exhaustion path validates+discards dead connections (F4); `seam_runtime/storage.py` decorates the six write methods with `retry_db_operation` so transient `database is locked`/`cannot commit` retries while non-transient errors (e.g. IntegrityError) propagate unchanged (F2). 9 new tests in `tests/audit/test_pool_concurrency.py`. Remaining audit follow-ups: S1 (shell argv execution — highest-value security), F5 (orphan-edge span:/pack:), B5 (fixture_hash divergence), B6 (benchmark determinism), beam/longmemeval durable-archive port, and audit #3 retrieval scoring (gated on restoring `locomo10.json`). Supersedes #269 below.
- Prior handoff `HISTORY#269` - audit-driven correctness slice on `audit/locomo-retrieval-bundle-270` (uncommitted in working tree at handoff time). Landed three audit fixes with 20 new tests: (1) `seam_runtime/pool.py` reset-on-return rollback closing the connection-contamination P1; (2) `seam bench publish` wiring the previously-dead `validate_publication_readiness` gate plus optional `SEAM_BENCHMARK_SIGNING_KEY` HMAC tamper-evidence in `seam_runtime/benchmark_integrity.py`; (3) durable LoCoMo result archiving to `benchmarks/runs/locomo/` (override `SEAM_BENCH_RESULTS_DIR`) with atomic+fsync+verified writes and per-scope `.partial.json` checkpoints on both sequential and parallel runner paths, so a multi-hour run is no longer lost to /tmp. Full audit at `docs/audits/2026-05-28-deep-health-audit.md`. Deferred: audit #3 (retrieval scoring RRF/semantic/BM25/reranker/temporal) pending restoration of `locomo10.json` and a measured dev-slice run; same durable-archive treatment still owed to the beam/longmemeval runners. Supersedes the prior handoff below.
- Prior handoff `HISTORY#268` - completed an unrecorded Qwen hardening pass on `feat/qwen-hardening-completion`. Replaced the unfinished `ShutdownManager` in `seam_runtime/server.py` with `ShutdownState` + `ShutdownMiddleware` + `_cleanup_runtime` + `_shutdown_timeout_from_env` matching the test API; defined module-scope `ALLOWED_SHELL_PATHS` / `ALLOWED_SHELL_COMMANDS` / `BLOCKED_SHELL_COMMANDS` frozensets in `seam_runtime/dashboard.py` plus `_validate_shell_executable` / `_validate_shell_command` / `_validate_shell_cwd` / `_get_shell_timeout` helpers; reverted `seam_runtime/storage.py` to HEAD then surgically re-applied only pool integration with `check_same_thread=False` so the merge-corrupted retrieval-event and improvement-proposal methods are restored. Kept Qwen's `seam_runtime/pool.py` + `seam_runtime/retry.py` + 3 audit test files (125 new tests). Added a Cut-off Recovery section to `AGENTS.md` applying to all agents and a new `QWEN.md` stub. Suite: 653 passed, 0 skipped, 0 failed (pgvector real-adapter tests run with ephemeral docker compose pgvector then torn down). Supersedes #267.
- Prior handoff `HISTORY#267` - polish + bugfix pass merged via PR #39: storage orphan-edge cleanup expansion across all record prefixes, batch_ir LIMIT/OFFSET consistency, temporal.parse_iso slice fix, retrieval LIKE escape, deterministic textual-field ordering, cosine docstrings, new `tools/history/retention.py` snapshot pruning CLI, pyproject upper bounds across optional extras, external-memory-benchmarks workflow bumped to Python 3.12, webui dead pane/component prune + HealthResponse cleanup, AGENTS.md topic vocab expansion.
- Earlier handoff `HISTORY#266` - H2 substrate slice 5 improvement_review gate; H2 phase 2 substrate complete. Slice 5 adds two append-only SQLite tables to `SQLiteStore` (`improvement_proposal` for the proposal body and `proposal_decision` for status transitions including the initial pending), `seam_runtime/improvement.py` with `validate_proposal()` (checks evidence `case_ids` against the slice-4 holdout pool and flags violations) plus `proposal_blocks_promotion()` (returns True unless latest status is approved AND no violation), and `tools/h2/improvement_review.py` CLI with subcommands `propose / list / show / approve / reject / summary` all supporting `--json`. Append-only: prior decisions are preserved so reverse decisions leave both rows in the audit trail. Hard rule preserved: slice 5 never writes to AGENTS.md / REPO_LEDGER.md / PROJECT_STATUS.md (enforced by a runtime test that drives the full CLI workflow in tmp and asserts those filenames never appear). 22 new tests in `tests/audit/test_h2_improvement_review.py`. H2 phase 2 substrate work (slices 1-5) is complete on `main`: substrate is writable both live (slice 2) and from history (slice 3), partitioned (slice 4), and gated for promotion (slice 5). PR #37 (HISTORY#265 slice 4) merged into `main` as commit `ed288d7`; PR #36 (HISTORY#263-#264) merged as `eeadf1c`; PR #34 (HISTORY#251-#261) merged as `c2dd45d`. `HISTORY#250` remains the GitHub PR stack closeout; `HISTORY#246` records the active GitHub ruleset `Protect main (PR + hygiene gates)`, and `HISTORY#249` records the `AGENTS.md` PR workflow for future agents. Materialising `benchmarks/external/locomo/holdout_assignment.json` against the real LoCoMo dataset, producing the first actual ranking-policy proposals from substrate evidence, and Step 4 final paid measurement all remain operator-gated.
- A fresh Linux clone should run `sh ./installers/install_seam_linux.sh --dev`, then verify local `HEAD` equals `origin/main` before starting new work.
- GitHub PR state as of 2026-05-18: PRs #22, #18, #23, #25 (SOP 0), #26, #27 (SOP 1), #28 (SOP 2), #29 (SOPs 3+4), and #30 (production readiness remediation) merged. Track I (SOPs 0-4) is complete on `main`. PR #19 is still draft, conflicting, and must be treated as a partial extraction source because its branch contains private-session-link material in commit metadata. PR #24 (Track I 5-SOP handoff series) was draft and is superseded.
- **Track I COMPLETE milestone.** Next track is the operator's choice per ROADMAP.md: Track J (Prompt Codec), Track K (Trust/Security/Auditability + BIL bundles), Track L (Agent/Skills Compiler), or Track H Phase 2-4 (improvement streams, retrieval integration, generalized library streams). Do not resume from already-merged branches or stale squash-merged PR refs. Do not propose or start Tracks J/K/L without operator direction.

## What Is Stable

- Core runtime paths (compile, verify, persist, search, context, benchmark)
- Textual dashboard (interactive TUI, chat, slash palette, reload command, MIRL animation, independent pane scrolling including Settings and Overview, ExplorerTree navigation, status bar, colored Rich markup panels, focus zoom toggle, Settings tab, and live Overview health bars)
- Dashboard installers: `seam-dash` shim on Windows (`.cmd`) and POSIX; `seam-dash` entrypoint in `pyproject.toml`
- Linux installer modes: `sh ./installers/install_seam_linux.sh` for global user commands, `sh ./installers/install_seam_linux.sh --dev` for repo-local Python development bootstrap and SEAM continuity checks.
- Dashboard launcher: `scripts/windows/launch_dashboard.bat` + `launch_dashboard.ps1`; propagates pgvector config from `SEAM_LOCAL_ENV` or a private Documents `SEAM\local\.env`
- pgvector real adapter: Docker Compose service `seam-pgvector` (image `pgvector/pgvector:0.8.2-pg18-trixie`, port 55432)
- Dashboard snapshot/smoke-test behavior
- Benchmark bundle verification, diff, gate, holdout workflow, and Windows GitHub Actions workflow (see HISTORY#095)
- Benchmark Integrity Level Phase 1 workflow: `seam bench inspect` reports BIL-0 for raw benchmark results, `seam bench seal --level BIL-1|BIL-2` writes deterministic bundles, and `seam bench verify` checks stable result hashes, BIL-2 manifest hashes, and BIL-2 manifest/result consistency. Stub-judge results require explicit `--allow-stub-seal` above BIL-0.
- `seam bench external --plan` CLI alias and `benchmarks/registry/memory_benchmarks.json` registry
- External memory benchmark suite (Track I complete): `seam bench external --quickstart locomo` with `--adapter {seam|mem0|zep}` and `--judge {stub|claude|openai}`; Mem0 comparator behind `seam[bench-mem0]` optional extra; Zep comparator behind `seam[bench-zep]` optional extra; three-way SEAM/Mem0/Zep scoring reproducible on quickstart fixture
- REST API skeleton: `seam serve`, `seam-server`, optional `server` extra, bearer-token protected endpoints, bounded request bodies, env-configurable process-local rate limiting, and remote authenticated bind guardrails
- RAG efficiency surface: `seam ingest <path> --persist`, `seam memory search`, `seam memory get`, `seam retrieve --mode mix`, document status rows, vector source-hash cache/stale checks, and `seam mcp stdio` MCP server
- MCP agent bridge: `seam mcp stdio` and `seam-mcp` expose 16 bounded, documented tools over standard MCP JSON-RPC for Gemini/Claude/Cursor-style clients; Gemini's project config starts it with `--ensure-pgvector` so Docker Compose pgvector is healthy before MCP tool discovery; `seam mcp serve` remains as the legacy JSON-lines bridge. Tools cover memory retrieval, controlled ingest, context, install diagnostics, stored HS/1 surfaces, index status, retrieval modes, and benchmark summaries; canonical `hs:<hex>` surface refs are required for surface tools.
- Interactive CLI shell: `seam shell` and `seam chat` provide the first REPL-style memory interface, with slash commands and prompt-ready context output
- Holographic Surface surface commands: `seam surface compile|encode|decode|verify|query|search|context|import`; `bw1`, `rgb`/`rgb24`, explicit `rgba32`, and explicit `rgba64`; `surface` benchmark exactness gate, stored lookup gate, stored query gate, repair gate, and repair-query gate
- Holographic Surface library commands: `seam surface store|list|show|repair`, plus `compile --store` and `encode --store` for SQLite metadata registration, redundant file-backed copies, and repair of missing/corrupt stored copies without committing generated user artifacts
- Durable history protocol (`AGENTS.md`, `HISTORY.md`, `HISTORY_INDEX.md`)
- Active/inactive separation: `docs/CODE_LAYOUT.md` maps live vs archived paths; `.rgignore` gates code search
- Token-bounded context loading via history snapshots and `tools.history.build_context_pack`
- Route-aware data classification through `tools/history/routing_manifest.json` and `docs/ledgers/`

## Active Focus

- Deferred: ~113 remaining assertTrue→specific-assertion replacements in test_seam.py (backlog card roadmap:track:F:asserttrue-scrub)
- Deferred: 10 low-priority backlog items catalogued under Track F (Phase 7)
- Reduce startup context overhead by relying on compact index + surgical history reads
- Preserve near-complete temporal history without loading all history into model context
- Keep maintenance, security, context, and runtime facts logically routed for AI search without duplicating chronology
- Make compression produce directly readable AI-native machine language, with opaque byte payloads used only as optional reconstruction/integrity backing layers
- Treat SEAM-HS/1 Holographic Surface as a queryable visual snapshot layer for MIRL/RC payloads, not as free compression or a replacement for SQLite truth
- Make the full functional visual-memory loop shippable: documents compile into directly readable MIRL/RC, MIRL/RC packs into SEAM-HS/1 PNG surfaces, stored surfaces remain addressable by metadata/hash, query/context can read the embedded payload directly from the image surface without restoring the original document, and the surface benchmark gates stored lookup plus repair at 100%
- Keep this private GitHub repo as the source-of-truth home for runtime files while leaving generated/operator user surfaces out of git unless deliberately promoted as fixtures or docs assets; future user-file sets belong in a separate repo
- Keep roadmap execution tied to history entries and supersedes chains
- Turn the competitive plan into shippable surfaces: finish README/install polish, graph/vector/mix retrieval hardening, agent bridge docs, and benchmark coverage without breaking existing CLI aliases
- Build the Agent Compiler workstream from `docs/roadmap/AGENT_COMPILER.md`: compile canonical SEAM protocol into model-specific adapters, benchmark those adapters, and audit installed local skills before applying changes.
- Continue hardening the `webui/` browser dashboard (served by `seam serve`/`seam webui` from `seam_runtime/webui/`): the static `dashboard.html` IDE shell is the shipped working UI; finish or replace the incomplete `webui/src/` React rewrite, and deepen live REST coverage without regressing graphs, settings, terminal, or chat.
- Turn the SEAM CLI into a first-class agent CLI like Gemini/Claude/Codex CLI: keep `seam shell` as the entrypoint, then add model routing, tool execution, repo/context awareness, command history, and guardrails on top of SEAM memory.
- Continue feature delivery without reintroducing duplicated continuity text
- Run real-adapter validation through guarded scripts to enforce resource ceilings and automatic service cleanup
- Legacy roadmap entries `HISTORY#028`-`HISTORY#047` remain append-only planning cards. `HISTORY#036` (holdout suites), `HISTORY#037` (benchmark diff tooling), and `HISTORY#046` (REST API surface) are implemented and superseded by `HISTORY#152`, `HISTORY#153`, and `HISTORY#154`; use `ROADMAP.md` Recommended Course for current priority.
- Track H Context Streams Protocol is planned (Phase 1 = H1 substrate; Phase 2 = H2 retrieval-feedback subset now). Full design captured in `docs/roadmap/CONTEXT_STREAMS.md`; HISTORY#243 starts the Track M subset immediately: retrieval-event capture, stale bundle backfill, current no-paid labels, dev/holdout split, and structured experience lessons. Broader stream retrieval integration (H3) and generalized library streams (H4) remain later.

## Operational Baseline

- Use `scripts/windows/launch_dashboard.bat` (wraps `launch_dashboard.ps1`) to start the dashboard on Windows with pgvector configured. Use `/reload` or `reload` inside the dashboard to rebuild dashboard panels, metrics, and chart state without restarting.
- Use `scripts/run_real_adapters_guarded.ps1` for end-to-end real adapter checks.
- Use `scripts/run_guarded.ps1` for heavy local commands where CPU/RAM/disk guardrails are needed.
- Use `scripts/store_benchmark.ps1` to archive benchmark runs under Documents with sequence+time folders, run index, and publication metadata/hashes.
- Use `seam benchmark diff <run-a> <run-b>` before claiming a benchmark improvement, `seam benchmark gate <bundle> [--baseline <run-a>]` before merge/release, and `seam benchmark run --holdout --confirm-holdout` only for publish-time audits.
- Use `python -m tools.history.build_context_pack --topics <tags> --latest <n> --token-budget <budget>` for bounded task context.
- Use `python -m tools.history.verify_continuity` before ending a changed session.
- Use `python -m tools.history.verify_routing` after changing data classifications or ledgers.
- Default memory guardrails are `82%` warning and `90%` hard limit.
- pgvector Docker Compose: `docker compose --env-file <private-env> up -d pgvector`; container `seam-pgvector`; port 55432; credentials stay outside the repo. Agent MCP startup can automate this with `seam-mcp --ensure-pgvector`.

## Working Rule

When resuming:

1. Read `PROJECT_STATUS.md`.
2. Read `REPO_LEDGER.md`.
3. Read `HISTORY_INDEX.md`.
4. Read `docs/CODE_LAYOUT.md`.
5. Read `docs/DATA_ROUTING.md` when the task touches history, ledgers, maintenance records, routing, context budget, or auditability.
6. Pull only required `HISTORY.md` entries by index offsets or with `python -m tools.history.build_context_pack`.
